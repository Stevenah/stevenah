import React, { Component } from 'react';
import "style/PhotoBox.scss"

class PhotoBox extends Component {
    render() {
        const {photo} = this.props;
        return (
            <div className="photobox-container">  
            </div>
        );
    }
}

export default PhotoBox;